// background.js

chrome.runtime.onInstalled.addListener(function () {
    // Set up an event listener for when the popup is shown
    chrome.runtime.onConnect.addListener(function (port) {
        
    });
});


